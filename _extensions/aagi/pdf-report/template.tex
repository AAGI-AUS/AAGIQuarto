% AAGI Full Report — Quarto LaTeX template
% Uses shared partials from the partials/ directory.
\documentclass[12pt]{article}

${ partials/_fonts.tex }$
${ partials/_packages.tex }$
${ partials/_pandoc-compat.tex }$
${ partials/_pagestyle-shared.tex }$

% ------------------------------------------------------------------
% firstpage style: AAGI logo top-right; full Partners banner footer
% ------------------------------------------------------------------
\fancypagestyle{firstpage}{%
  \fancyhf{}
  \fancyhead[R]{%
    \begin{minipage}[t]{0.3\textwidth}
      \raggedleft
      \includegraphics[height=2.3cm]{AAGI_logo}%
    \end{minipage}%
  }
  \fancyfoot[C]{%
    \rule{\textwidth}{0.4pt}\\[-10pt]%
    \centering
    \includegraphics[width=\textwidth]{Partners}%
  }
  \renewcommand{\headrulewidth}{0pt}
  \renewcommand{\footrulewidth}{0pt}
}

% ------------------------------------------------------------------
% otherpages style: title in header; page number + Partners footer
% ------------------------------------------------------------------
\fancypagestyle{otherpages}{%
  \fancyhf{}
  \fancyhead[R]{{\aagiheaderstyle $title$}}
  \fancyfoot[R]{{\sffamily \thepage}}
  \fancyfoot[C]{%
    \rule{\textwidth}{0.4pt}\\[-10pt]%
    \includegraphics[width=\textwidth]{Partners}%
  }
  \renewcommand{\headrulewidth}{0pt}
  \renewcommand{\footrulewidth}{0pt}
}

% ------------------------------------------------------------------
% Quarto header-includes pass-through
% ------------------------------------------------------------------
$for(header-includes)$
$header-includes$
$endfor$

\begin{document}

% ------------------------------------------------------------------
% Title page (opt-in via has-title-page: true in YAML)
% ------------------------------------------------------------------
$if(has-title-page)$
\thispagestyle{firstpage}
\begin{titlepage}
  \newgeometry{top=40mm,left=20mm,right=20mm,bottom=20mm}

  \vspace*{10mm}
  {\color{aagiteal}\rule{\textwidth}{2pt}}
  \vspace{6mm}

  {\sffamily\huge\bfseries\color{aagiblack} $title$\par}
  \vspace{4mm}

  $if(subtitle)$
  {\sffamily\Large\color{aagiteal} $subtitle$\par}
  \vspace{4mm}
  $endif$

  {\color{aagiteal}\rule{\textwidth}{0.5pt}}
  \vspace{6mm}

  $for(author)$
  {\sffamily\large $author$\par}
  $endfor$

  \vspace{4mm}
  $if(university)$
  {\sffamily\normalsize\color{aagiblack} $university$\par}
  $endif$
  $if(correspondence-email)$
  {\sffamily\normalsize Correspondence: \href{mailto:$correspondence-email$}{$correspondence-email$}\par}
  $endif$

  \vfill
  {\sffamily\normalsize $date$\par}
  \restoregeometry
\end{titlepage}
\cleardoublepage
$else$
\thispagestyle{firstpage}
$endif$

\pagestyle{otherpages}

% ------------------------------------------------------------------
% Table of contents (opt-in via toc: true in YAML)
% ------------------------------------------------------------------
$if(toc)$
{
  \hypersetup{linkcolor=aagiblack}
  \tableofcontents
  \newpage
}
$endif$

$body$

\end{document}
