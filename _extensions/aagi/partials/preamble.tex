% -------------------------
% Engines + fonts
% -------------------------
\usepackage{iftex}

\ifPDFTeX
  % Fallback (should not be used)
  \usepackage{lmodern}
  \renewcommand{\familydefault}{\sfdefault}
\else
  \usepackage{fontspec}
  \usepackage{microtype}

  % --- Sans font ---
  \IfFontExistsTF{Proxima Nova}{
    \setsansfont[Scale=MatchLowercase]{Proxima Nova}
  }{
    \IfFontExistsTF{Arial}{
      \setsansfont[Scale=MatchLowercase]{Arial}
    }{
      \setsansfont[Scale=MatchLowercase]{TeX Gyre Heros}
    }
  }

  \renewcommand{\familydefault}{\sfdefault}

  % --- Monospace (bundled, deterministic) ---
  \setmonofont[
    Path = assets/fonts/,
    Extension = .ttf,
    UprightFont = FiraCode-Regular,
    BoldFont    = FiraCode-Bold,
    ItalicFont  = FiraCode-Light,
    Scale = MatchLowercase
  ]{FiraCode-Regular}
\fi

% -------------------------
% Core packages
% -------------------------
\usepackage[paper=a4paper,top=22mm,left=12mm,right=12mm,bottom=22mm]{geometry}
\usepackage{graphicx}
\DeclareGraphicsExtensions{.pdf,.png,.jpg}

\usepackage{amsmath,amssymb}
\usepackage{parskip}

% Load xcolor with dvipsnames only if not already loaded; if already loaded,
% add the dvipsnames option retroactively via xcolor's own mechanism.
\@ifpackageloaded{xcolor}{%
  % xcolor already loaded by Quarto — ensure dvipsnames colours are available
  \@ifpackagewith{xcolor}{dvipsnames}{}{%
    \PassOptionsToPackage{dvipsnames}{xcolor}%
  }%
}{%
  \usepackage[dvipsnames]{xcolor}%
}

\usepackage{hyperref}
\usepackage{csquotes}

% -------------------------
% Colours (define once, no duplicates)
% -------------------------
\providecolor{aagiteal}{HTML}{00808B}
\providecolor{aagigreen}{HTML}{B6D438}
\providecolor{aagiblack}{HTML}{414042}
\providecolor{aagigrey}{HTML}{F2F2F2}

% -------------------------
% Pandoc / CSL compatibility
% -------------------------
\providecommand{\citeproc}[2]{#2}
\providecommand{\citeproctext}{}
\providecommand{\CSLLeftMargin}[1]{}
\providecommand{\CSLRightInline}[1]{#1}
\providecommand{\CSLBlock}[1]{#1}

\providecommand{\tightlist}{%
  \setlength{\itemsep}{0pt}
  \setlength{\parskip}{0pt}
}

\newenvironment{CSLReferences}[2]
 {%
  \begin{list}{}{%
    \setlength{\leftmargin}{1.5em}
    \setlength{\itemindent}{-1.5em}
    \setlength{\labelwidth}{0pt}
    \setlength{\labelsep}{0pt}
    \renewcommand{\makelabel}[1]{}
  }
 }
 {\end{list}}
